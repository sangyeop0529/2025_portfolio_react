import React from "react";
import style from "./Detail.module.css";

const Detail = () => {
  return (
    <section id="projects" className={style.section}>
      <div className={`${style.container} container`}>작업중...</div>
    </section>
  );
};

export default Detail;
