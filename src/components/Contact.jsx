import React from "react";
import style from "./Contact.module.css";

const Contact = () => {
  return (
    <section className={style.section}>
      <div className={`${style.container} container`}>Experience</div>
    </section>
  );
};

export default Contact;
