import React from "react";
import style from "./Contact.module.css";

const Contact = () => {
  return (
    <section className={style.section} id="contact">
      <div className={`${style.container} container`}>Contact</div>
    </section>
  );
};

export default Contact;
